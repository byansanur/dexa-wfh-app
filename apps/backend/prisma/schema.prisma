datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id        String       @id @default(uuid()) @db.Uuid
  email     String       @unique @db.VarChar(255)
  password  String       @db.VarChar(255)
  name      String       @db.VarChar(255)
  position  String?      @db.VarChar(100)
  phone     String?      @db.VarChar(20)
  photoUrl  String?      @map("photo_url")
  role           String       @db.VarChar(20)
  attendanceType String       @default("SINGLE") @db.VarChar(10)
  officeHourStart String      @default("08:00") @db.VarChar(10)
  createdAt      DateTime     @default(now()) @map("created_at")
  updatedAt DateTime     @default(now()) @updatedAt @map("updated_at")
  Attendances Attendance[]

  @@map("users")
}

model Attendance {
  id        String    @id @default(uuid()) @db.Uuid
  userId    String    @map("user_id") @db.Uuid
  date      DateTime  @db.Date
  clockIn   DateTime? @map("clock_in")
  clockOut  DateTime? @map("clock_out")
  clockInLocation String? @map("clock_in_location") @db.VarChar(100)
  clockOutLocation String? @map("clock_out_location") @db.VarChar(100)
  createdAt DateTime  @default(now()) @map("created_at")
  updatedAt DateTime  @default(now()) @updatedAt @map("updated_at")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("attendances")
}
